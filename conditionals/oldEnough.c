#include <stdio.h>

int age[5];

int main(void){
    printf("how old are you?\n");
    if (age >= 18){
        printf("you are old enough to vote, you're old enough to drive, you're old enough to have a learners permit, you're !\n");
    }else if (age >= )
    }
    return 0;
}