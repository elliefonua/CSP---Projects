#include <stdio.h>
#include <string.h>

int main(void){
    char lname[] = "Ellie";
    char prefix[] = "<<<";
    char fname[] = ">>>"
    strcat(prefix, fname);
    printf(prefix, lname, fname)
    strcat(prefix, lname)
    printf("%s", prefix);
    return 0;
}